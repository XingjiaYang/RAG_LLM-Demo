# Direct dependencies for full local development.
# Keep transitive dependencies out of this file; use a lock file if exact
# environment reproduction is needed.

-r requirements.api.txt

# Needed only when serving the LLM locally outside Docker.
vllm
